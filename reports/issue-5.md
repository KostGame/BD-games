# Issue #5

## Что изменено

- В `tests/db-basics.js` заменен набор вопросов и вариантов ответов на новый набор из Issue #5.
- Сохранено перемешивание вариантов при каждом рендере и после сброса теста.
- Логика подсчета результата и блока разбора ответов оставлена без изменений.

## Почему старый вариант был плохим

- В старом наборе правильные ответы слишком легко угадывались по длине и позиции.
- Часть отвлекающих вариантов выглядела неестественно и выбивалась из темы.
- На проде оставался старый набор, хотя задача уже требовала обновить вопросы.

## Что проверено

- Локально выполнен `node --check tests/db-basics.js`.
- Выполнен smoke-test в Node VM:
  - 10 вопросов;
  - 40 вариантов;
  - shuffle меняет порядок вариантов;
  - результат считается корректно;
  - reset заново перемешивает варианты.
- Выполнен `git diff --check`.
- После push проверен `origin/main` через `git show origin/main:tests/db-basics.js`:
  - новый набор присутствует;
  - старые формулировки отсутствуют.

## Итог

- Коммит с исправлением: `cd40411`.
- Изменения отправлены в `main`.
- GitHub Pages для проекта остается на `main`.

## Ссылка на прод

https://kostgame.github.io/BD-games/tests/db-basics.html
